<!DOCTYPE html>
<html">
  <head>

    <title>icra vakti</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>

    <!-- Le styles -->
    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>
    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

  </head>

  <body>_
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <%= link_to :icra_vakti, root_path, class: "brand" %>
          <div class="nav-collapse">
            <ul class="nav">
              <!--li class="active"><a href="#">Anasayfa</a></li-->
            </ul>
          </div><!--/.nav-collapse -->
          <!-- Login Logout -->
          <div class="nav-collapse" id="user_nav">
              <ul class="nav">
                <li><%= link_to "param kimde?", loans_path %></li>
                <li><%= link_to "arkadaslarim", borrowers_path %></li>
              </ul>
            <% if user_signed_in? %>
              <%= current_user.email %> olarak giriş yaptınız
              <%= link_to "Çıkış Yap", destroy_user_session_path, :method => :delete %>
            <% else %>
              <%= link_to image_tag("facebook.png"), user_omniauth_authorize_path(:facebook) %>
              <%= link_to image_tag("twitter.png"), user_omniauth_authorize_path(:twitter) %>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <div class="container">
  <% if flash[:alert] %>
    <p id="error"><%= flash[:alert] %></p>
  <% end %>
  <% if flash[:notice] %>
    <p id="notice"><%= flash[:notice] %></p>
  <% end %>

      <%= yield %>

    </div> <!-- /container -->
  </body>
</html>
